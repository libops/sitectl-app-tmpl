name: goreleaser

on:
  workflow_dispatch:
    inputs:
      release-mode:
        description: Release mode; full supports tag releases and default-branch recovery
        required: true
        type: choice
        default: homebrew-only
        options:
          - homebrew-only
          - full
      release-version:
        description: Existing vMAJOR.MINOR.PATCH tag; required for default-branch full recovery
        required: false
        type: string
  push:
    tags:
      - "*"

permissions:
  contents: write
  id-token: write

jobs:
  lint-test:
    if: github.ref_name != 'v0.0.0'
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          ref: ${{ inputs.release-version || github.ref }}
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: "1.26.5"

      - name: golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9
        with:
          version: v2.12.2
          install-only: true

      - name: lint
        run: make lint

      - name: unit test
        run: make test

  goreleaser:
    needs: lint-test
    if: github.ref_name != 'v0.0.0'
    uses: libops/.github/.github/workflows/sitectl-plugin-goreleaser.yaml@e1e30b58c9c566f72b22f03e637cd5218d635727 # main
    permissions:
      contents: write
      id-token: write
    secrets: inherit
    with:
      go-version: "1.26.5"
      release-mode: ${{ github.ref_type == 'tag' && 'full' || inputs.release-mode }}
      release-version: ${{ inputs.release-version }}
      sitectl-ref: 65cfde137a58ba14aaa9a1512d88b943888872f3 # v1.0.0
      package-name: sitectl-app-tmpl
      publish-package-repo: false
