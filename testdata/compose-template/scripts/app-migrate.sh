#!/bin/sh

set -eu

echo 'ACTION REQUIRED: replace this fail-closed template migration program with the application-supported database migration or an explicit manual migration gate before release.' >&2
exit 1
